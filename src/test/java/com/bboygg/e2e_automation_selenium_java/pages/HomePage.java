package com.bboygg.e2e_automation_selenium_java.pages;

import org.openqa.selenium.WebDriver;
public class HomePage extends BasePage{

    public HomePage(WebDriver driver) {
        super(driver);
    }

    /**
     * This class represents the Home Page. It follows certain naming conventions:
     *
     * Text Elements: Elements that represent text on the UI are suffixed with 'Txt'.
     * Example: TitleTxt
     *
     * Input Elements: Elements that represent input fields are suffixed with 'Input'.
     * Example: nameInput
     *
     * Button Elements: Elements that represent buttons are suffixed with 'Btn'.
     * Example: shareBtn
     */

    // Page methods for the Contact Page
    /**
     * This method enters the user's name into the name input field.
     *
     * @param name The name of the user.
     */


}
